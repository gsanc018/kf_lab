{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run example",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/examples/00_smoketest.py",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}